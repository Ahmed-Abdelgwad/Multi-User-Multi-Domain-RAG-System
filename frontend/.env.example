# Base URL of the FastAPI backend. Defaults to the docker-compose port
# mapping (localhost:8001 -> container :8000). Use localhost:8000 for
# the README's sqlite/uvicorn-local run instead.
VITE_API_BASE_URL=http://localhost:8001
